class LocalFileStorage {
    // Constructor with fs module and configuration where to look for files.
    // Get(filePath) returns a readable stream ready to pipe to response.
}

module.exports = LocalFileStorage;
