class ApiController {
    // Relay() relays request to API controller.
    //   TODO Review if this is the right way...redirect?
    // Detects when API service is down.
}

module.exports = ApiController;
