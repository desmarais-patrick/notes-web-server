class FileController {
    // Get(request, response) pipes request file to response.
    //   - Builds headers and prepares response.
    //   - Caches file content and detect changes for next request.
    //   - Detects if file storage is down or inaccessible.
    //   - Detects if no file found.
}

module.exports = FileController;
